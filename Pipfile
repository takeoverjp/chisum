[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "tsdraw"

[packages]
click = "*"

[dev-packages]
flake8 = "*"

[requires]
python_version = "3.8"

[scripts]
tsdraw = "python src/main.py"
test = "python -m unittest discover -v -s test"
format = "autopep8 -ivr ."
lint = "flake8 --show-source ."
